一、Demo 项目总体定位（给 AI 的总 Prompt）

用于所有子 Prompt 的「系统级前置说明」

你正在生成一个【前端微前端治理方案 Demo 项目】。
该项目仅用于方案讲解和评审，不用于真实开发，用户不会查看源代码。

项目背景：

-   一个大型企业级中台系统
-   技术背景：Vue3 + Vite + Monorepo + 微前端（类似无界）
-   存在几十个前端子应用，支持独立升级
-   存在复杂的菜单、权限、路由、能力治理问题

Demo 目标：

-   向产品经理和架构师清晰展示：
    1. 前端资源如何在代码中声明
    2. 构建阶段如何生成 Manifest 与能力目录
    3. 后台如何管理能力目录（含 diff）
    4. 权限如何基于能力授权
    5. 菜单如何基于能力编排
    6. 运行时如何加载子应用、恢复路径、拉起依赖服务

展示重点：

-   概念清晰
-   流程完整
-   页面结构直观
-   不关心真实代码可运行性

二、Demo 项目页面结构 Prompt（整体导航）
请生成一个 Demo 系统的整体页面结构，包含左侧导航和主内容区。

左侧导航包含以下模块：

1. Resource 声明示意（代码视图）
2. Manifest & 能力目录
3. 能力 Diff 管理
4. 权限管理
5. 菜单管理
6. 服务依赖管理
7. 整体流程示意

每个模块都是“展示型页面”，不需要真实交互，只需表达结构和信息关系。

三、Resource 代码结构展示页面 Prompt

这是给产品和架构师看的“前端能力源头”

生成一个页面，模拟“前端子应用中的 Resource 声明代码结构”。

页面左侧展示一个类似代码编辑器的区域（只做展示）：

-   文件名：resources.ts
-   内容是 JSON / TypeScript 风格的结构

示例结构包括：

-   应用标识 appId
-   页面资源 pageResources
-   每个页面包含：
    -   resourceKey
    -   title（页面名称）
    -   description（业务能力说明）
    -   routePath
    -   buttons（按钮能力）
    -   dependencies（依赖的其他前端子应用 / 后端微服务）

页面右侧用自然语言解释：

-   这不是菜单
-   这是“能力声明”
-   是构建 Manifest 和能力目录的唯一来源

强调：

“能力先于菜单存在”

“页面 ≠ 路由 ≠ 菜单”

四、Manifest & 能力目录管理页面 Prompt
生成一个“能力目录管理后台”页面。

页面分为上下两部分：

上半部分：Manifest 列表

-   每一行代表一个子应用
-   字段包括：
    -   子应用 ID
    -   当前版本号
    -   构建时间
    -   Manifest 状态（已解析 / 待处理）
    -   是否与能力目录同步

下半部分：能力目录表（Capability Catalog）

-   表格字段：
    -   capabilityKey
    -   名称（产品可编辑）
    -   描述（产品可编辑）
    -   来源子应用
    -   页面 / 按钮类型
    -   当前版本
    -   状态（新增 / 变更 / 已废弃）

强调：

-   Manifest 是“技术事实”
-   能力目录是“产品视角”
-   能力目录不会被 Manifest 直接覆盖

五、能力 Diff 管理页面 Prompt（重点）
生成一个“能力 Diff 对比页面”。

页面展示：

-   左侧：当前线上能力目录
-   右侧：新版本 Manifest 解析结果

Diff 形式包括：

-   新增能力（绿色）
-   变更能力（黄色）
-   删除能力（红色，仅标记，不强制阻断）

每一条 Diff 展示：

-   capabilityKey
-   变化类型
-   变化字段（如 routePath / dependencies）
-   影响范围提示（哪些菜单 / 角色使用）

页面底部说明：

-   系统不强制中断发布
-   Diff 用于人工评估和验证
-   适用于“停机升级”场景

六、权限管理页面 Prompt
生成一个权限管理页面。

页面结构：

-   左侧：角色列表（如：管理员、运营、审计员）
-   中间：能力树（来自能力目录）
-   右侧：已授权能力列表

能力树特点：

-   按 domain / category 分组
-   页面能力下可展开按钮能力

强调：

-   权限粒度是“能力”
-   不直接绑定路由或菜单

七、菜单管理页面 Prompt
生成一个菜单管理页面。

页面展示：

-   菜单树结构（支持多层级）
-   每个菜单节点可选择：
    -   一个或多个能力（页面能力）
    -   或外部链接

右侧说明区：

-   当前菜单关联的能力
-   能力所属子应用
-   点击后会拉起哪个子应用、哪个页面

强调：

-   菜单是“导航结构”
-   能力是“可访问的功能”
-   一个能力可被多个菜单复用

八、服务依赖管理页面 Prompt（新需求重点）
生成一个“服务依赖管理页面”。

页面展示一个能力或页面的依赖关系图：

-   前端子应用依赖
-   后端微服务依赖

每个依赖包含：

-   服务标识
-   类型（前端 / 后端）
-   启动方式（自动拉起 / 仅检测）
-   当前运行状态

页面说明：

-   前端启动时可根据依赖声明
-   自动拉起本地或测试环境的后端服务
-   用于开发 / 联调 / 演示环境

九、整体流程示意图 Prompt（非常关键）
图 1：从代码到上线的治理流程
生成一张流程图，描述以下流程：

1. 开发在子应用中声明 Resource
2. 构建阶段生成 Manifest
3. Manifest 上报治理后台
4. 系统生成 Diff
5. 产品编辑能力目录
6. 管理员配置权限
7. 配置菜单
8. 用户访问系统，按能力加载子应用

要求：

-   使用泳道区分：开发 / 构建系统 / 后台 / 运行时
-   每一步有清晰文本说明

图 2：运行时路由与子应用加载关系
生成一张架构图，描述运行时：

-   浏览器地址栏包含完整路径
-   底座解析路径
-   确定子应用
-   加载对应版本的子应用
-   注册子应用内部路由
-   渲染目标页面

强调：

-   刷新可恢复
-   不依赖点击行为

